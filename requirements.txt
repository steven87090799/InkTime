Flask==3.1.3
requests==2.33.0
Pillow==12.3.0
pillow-heif==1.5.0
cryptography==50.0.0
gunicorn==23.0.0
psutil==7.0.0
fonttools==4.63.0
opencv-python-headless==4.13.0.92
# 2.2.6 is the newest shared line verified with Python 3.10/3.12 and OpenCV 4.13.
# Newer NumPy lines no longer share the project's complete supported interpreter matrix.
numpy==2.2.6
